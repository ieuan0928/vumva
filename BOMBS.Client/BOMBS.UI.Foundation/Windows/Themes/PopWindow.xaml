<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:BOMBS.UI.Foundation.Windows">
    <Style TargetType="{x:Type local:PopWindow}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PopWindow}">
                    <ControlTemplate.Resources>
                        <FontFamily x:Key="FontAwesome">/Fonts/fontawesome-webfont.ttf#FontAwesome</FontFamily>
                    </ControlTemplate.Resources>
                    <Grid>
                        <TextBlock Text="&#xf207;" Foreground="White" FontFamily="{StaticResource FontAwesome}" />
                        <ContentControl />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>